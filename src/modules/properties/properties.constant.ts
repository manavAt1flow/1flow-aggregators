export enum PROPERTIES_TYPE {
    USER = 'user',
    EVENT = 'event',
}

export enum DATA_TYPE {
    STRING = 'string',
    NUMBER = 'number',
    DATE = 'date',
    BOOLEAN = 'boolean',
    ARRAY = 'array'
}
